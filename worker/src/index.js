// Cloudflare Worker migration placeholder.
// Keep the Node/Express staging server working until the Worker routes are tested.
export default { async fetch() { return new Response('Maa Astrology API scaffold — migrate routes from server.js', {status: 501}); } };
